# 01 — Core UI Shell & Dark Design System

**What to build:** Establish the modern dark design system tokens, CSS variables, glassmorphism cards, glowing accent utilities, typography, and responsive App shell so all subsequent features render inside a sleek UI container.

**Blocked by:** None — can start immediately.

**Status:** ready-for-agent

- [ ] Define dark theme CSS custom properties (`:root` colors for `#0f172a`, `#1e293b`, cyan `#06b6d4`, violet `#8b5cf6`, text `#f8fafc`).
- [ ] Implement responsive App Layout container with Header, Navigation bar, and Main content area.
- [ ] Provide reusable CSS classes for glassmorphism panels, glowing action buttons, and animated status badges.
- [ ] Ensure full responsiveness across desktop and mobile screen sizes.
